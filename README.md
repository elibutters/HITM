Files are named in order of precedence. So, S1 comes first, then S2, S3, S4, and so on.

R2_STD_IVS_DFW_PARAMS and R@_STD_IVS_DFW are the same except the params one just has the DFW params in it

*Corrections:
add Attention
Review loss functions, IV can be over 1
VAE pre-processing
make sure all scalers have no look forward